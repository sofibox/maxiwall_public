#!/bin/bash
# Warning this is deprecated (suricata now has update tool without manually install python)
# Author: Arafat Ali | Email: webmaster@sofibox.com
# I use bash instead of python (the original code) because my python is setup only with secured environment. So it cannot detect suricata-update script and whenever I run suricata-update, i will get error. So this one fixed it:

ARGS="$*"
PYTHON_ENV_BIN="/home/emaxi32x/maxipy/bin/"
SCRIPT="suricata-update"

# Run the update:
"${PYTHON_ENV_BIN}/${SCRIPT}" "${ARGS}"
